import { afterEach, beforeEach, describe } from "vitest";
import { Match } from "../Match";

describe("Match", () => {
  let match: Match;

  beforeEach(() => {
    match = new Match();
  });

  afterEach(() => {
    // Cleanup if needed
  });
});
